# =====================
# ===== Main flow =====
# =====================

home:
  path: "/"
  handlers: home/home.handlers
  pageHeading: Sample home page
  view: home/home
  next: status

status:
  path: "/status"
  handlers: status/status.handlers
  pageHeading: Sample status page
  view: status/status
  next: single-option

single-option:
  path: "/single-option"
  handlers: single-option/single-option.handlers
  pageHeading: Single option page
  isQuestionPage: true
  view: common/select-one-option
  next: home